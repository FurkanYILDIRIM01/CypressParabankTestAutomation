class BaseLib{

    randomNummer(){
   
     return Math.floor(Math.random() * 1000000);
    }
     
   // deneme = Math.floor(Math.random() * 10);
   // return deneme;
 
 }
 
 export default BaseLib;